# 应用服务 WEB 访问端口
spring:
  application:
    name: ai-code-helper
server:
  port: 8081
  servlet:
    context-path: /api
langChain4j:
  community:
    dashscope:
      chat-model:
        model-name: qwen-max
        api-key: ${LANGCHAIN4J_API_KEY}
